// Turbopack alias target for optional x402 payment modules pulled in statically by
// RainbowKit's bundle (via the Coinbase Smart Wallet / Base Account connector), which
// SILENT doesn't use - standard wallet connect/sign only.
module.exports = {};
